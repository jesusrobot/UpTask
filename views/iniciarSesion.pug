<!DOCTYPE html>
html(lang="en")
  head
    include partials/header
  body.login
    .contenedor-formulario
      h1 Iniciar sesion en UpTask
      if locals.mensajes
        .alertas
          each categoria in Object.keys(locals.mensajes)
            each error in mensajes[categoria]
              .alerta(class=`${categoria}`)
                p= error
      form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion" require)
        .campo
          label(for="email") Email
          input(type="email" id="email" name="email" placeholder="Ingresa tu email" require value= email)
        .campo
          label(for="password") Password
          input(type="password" id="password" name="password" placeholder="Ingresa tu password" value= password)
        .campo.enviar
          input.boton(type="submit" value="Iniciar sesion")
        .campo.acciones
          a(href="/crear-cuenta") Crear una cuenta gratis 
          a(href='/reestablecer') ¿Se te olvido tu contraseña?
