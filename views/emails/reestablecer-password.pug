<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    style
      include ./email.css
      
  body
    .contenedor
      h2 Reestablecer password
      p Hola, Has solicitado reestablecer tu password, haz click en el siguiente enlace para reestablecerlo, este enlace es temporal, asi que si se vence vuelve a solicitarlo.
      a(href=`${resetUrl}`) Ir a Resetear el password
      p.ghost Si no puedes acceder a este enlace, visita #{resetUrl}
      p.ghost Si no solicitaste este e-mail, puedes ignorarlo
